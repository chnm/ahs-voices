<?php
$escape = $this->plugin('escapeHtml');
$translate = $this->plugin('translate');
$siteTitle = $site->title();
$this->htmlElement('html')->setAttribute('lang', $this->lang());
$this->headMeta()->setCharset('utf-8');
$this->headMeta()->appendName('viewport', 'width=device-width, initial-scale=1');
$this->headTitle($siteTitle)->setSeparator(' · ');
$this->headTitle()->append($this->setting('installation_title', 'Omeka S'));
$this->headLink()->prependStylesheet($this->assetUrl('css/style.css'));
$this->headLink()->prependStylesheet($this->assetUrl('css/iconfonts.css', 'Omeka'));
$this->headLink()->prependStylesheet('https://fonts.googleapis.com/css2?family=Bitter:wght@400;500;600;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap');
$this->headScript()->prependFile($this->assetUrl('js/global.js', 'Omeka'));
$this->headScript()->prependFile($this->assetUrl('vendor/jquery/jquery.min.js', 'Omeka'));
$this->jsTranslate();
$this->trigger('view.layout');
$userBar = $this->userBar();
?>
<?php echo $this->doctype(); ?>
<?php echo $this->htmlElement('html'); ?>
    <head>
        <?php echo $this->headMeta(); ?>
        <?php echo $this->headTitle(); ?>
        <?php echo $this->headLink(); ?>
        <?php echo $this->headStyle(); ?>
        <?php echo $this->headScript(); ?>
        <?php echo $this->partial('common/theme-config'); ?>
    </head>

    <?php echo $this->htmlElement('body'); ?>
        <a id="skipnav" href="#content"><?php echo $translate('Skip to main content'); ?></a>
        <?php echo $userBar; ?>

        <!-- Pre-header strip -->
        <div class="pre-header">
            <div class="container">
                <span><?php echo $translate('A project of the'); ?> <a href="https://arlhist.org" class="pre-header-highlight"><?php echo $translate('Arlington Historical Society'); ?></a></span>
                <span>
                    <a href="https://arlhist.org/visit/"><?php echo $translate('Visit AHS'); ?></a>
                    <a href="https://arlhist.org/support/donate/"><?php echo $translate('Donate'); ?></a>
                </span>
            </div>
        </div>

        <!-- Header -->
        <header class="site-header">
            <div class="container">
                <h1 class="site-title">
                    <a href="<?php echo $site->url(); ?>">
                    <?php $subtitle = $this->themeSetting('site_subtitle'); ?>
                    <?php if ($this->themeSetting('logo')): ?>
                        <img class="header-logo" src="<?php echo $this->themeSettingAssetUrl('logo'); ?>" alt="<?php echo $escape($siteTitle); ?>" />
                        <span class="header-divider" aria-hidden="true"></span>
                    <?php endif; ?>
                    <span class="wordmark <?php echo $subtitle ? 'wordmark-has-subtitle' : 'wordmark-title-only'; ?>">
                        <span class="wordmark-name"><?php echo $escape($siteTitle); ?></span>
                        <?php if ($subtitle): ?>
                        <span class="wordmark-subtitle"><?php echo $escape($subtitle); ?></span>
                        <?php endif; ?>
                    </span>
                    </a>
                </h1>
                <nav id="top-nav">
                    <button type="button" id="mobile-nav-toggle" aria-expanded="false" aria-label="<?php echo $translate('Toggle Menu'); ?>">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/>
                        </svg>
                    </button>
                    <?php echo $site->publicNav()->menu()->renderMenu(null, [
                        'maxDepth' => ($this->themeSetting('nav_depth') ?: 1) - 1,
                        'id' => 'top-nav-contents'
                    ]); ?>
                </nav>
            </div>
        </header>

        <!-- Main content -->
        <div id="content" role="main">
            <?php echo $this->content; ?>
        </div>

        <!-- Footer -->
        <footer class="site-footer">
            <div class="container">
                <div class="footer-grid">
                    <div class="footer-col">
                        <h4><?php echo $escape($siteTitle); ?></h4>
                        <?php $footerDesc = $this->themeSetting('footer_content'); ?>
                        <p style="font-size: 14px; line-height: 1.6;">
                            <?php echo $escape($footerDesc ?: 'Preserving and sharing the stories of Arlington, Virginia, through recorded interviews with residents.'); ?>
                        </p>
                    </div>
                    <div class="footer-col">
                        <h4><?php echo $translate('Explore'); ?></h4>
                        <a href="<?php echo $site->url(); ?>"><?php echo $translate('Home'); ?></a>
                        <a href="#"><?php echo $translate('Browse Interviews'); ?></a>
                        <a href="#"><?php echo $translate('Collections'); ?></a>
                        <a href="#"><?php echo $translate('Exhibits'); ?></a>
                    </div>
                    <div class="footer-col">
                        <h4><?php echo $translate('The Society'); ?></h4>
                        <a href="#"><?php echo $translate('About AHS'); ?></a>
                        <a href="#"><?php echo $translate('Visit Us'); ?></a>
                        <a href="#"><?php echo $translate('Donate'); ?></a>
                    </div>
                    <div class="footer-col">
                        <h4><?php echo $translate('Project'); ?></h4>
                        <a href="#"><?php echo $translate('About This Project'); ?></a>
                        <a href="#"><?php echo $translate('Contribute a Story'); ?></a>
                        <a href="#"><?php echo $translate('Rights & Permissions'); ?></a>
                    </div>
                </div>
                <div class="footer-bottom">
                    <?php echo sprintf($translate('&copy; %s %s. All rights reserved.'), date('Y'), $escape($siteTitle)); ?>
                </div>
            </div>
        </footer>
    </body>
</html>
