# Screenshots for managing-collections.md

Drop cropped screenshots here with these filenames. Each is referenced by an
HTML comment at its slot in `../../managing-collections.md`; once a file exists,
replace that comment with a normal image embed, e.g.:

```markdown
![Item sets list with the Add new item set button](images/collections/item-sets-list.png)
```

| Filename | What it should show |
|---|---|
| `item-sets-list.png` | The Item sets list; "Item sets" highlighted in the left menu; the "Add new item set" button top-right. |
| `item-set-values.png` | The Values tab with Title, Description, and Abstract filled in; the Dublin Core property group open on the right. |
| `item-set-thumbnail.png` | The Advanced tab, Thumbnail area, with a selected image. |
| `assign-to-site.png` | Sites → Arlington Voices → Resources → **Item sets** tab: the assigned collections list and the "add" panel on the right. |

Tip: crop out the browser address bar and the "Signed in as…" bar for a cleaner image.
